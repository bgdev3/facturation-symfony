{% extends 'base.html.twig' %}

{% block title %}New Facture{% endblock %}

{% block body %}
    <h1  class="text-center text-xl font-bold mb-6 mt-6">Nouvelle facture</h1>

    <div class=" max-w-4xl mx-auto">
        {{ include('facture/_form.html.twig') }}
    </div>

      <div class="w-full flex justify-center">
          <a href="{{ path('facture.index') }}" class="inline-block px-2.5 py-1.5 text-center bg-yellow-300 font-bold rounded-md text-dark text-xs hover:bg-yellow-100 transition-colors">Retour</a>
      </div>
  
{% endblock %}
