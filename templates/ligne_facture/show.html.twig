{% extends 'base.html.twig' %}

{% block title %}LigneFacture{% endblock %}

{% block body %}
    <h1>LigneFacture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligne_facture.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ ligne_facture.designation }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ ligne_facture.quantite }}</td>
            </tr>
            <tr>
                <th>PrixUnitaireHT</th>
                <td>{{ ligne_facture.prixUnitaireHT }}</td>
            </tr>
            <tr>
                <th>TauxTVA</th>
                <td>{{ ligne_facture.tauxTVA }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ligne_facture_index') }}">back to list</a>

    <a href="{{ path('app_ligne_facture_edit', {'id': ligne_facture.id}) }}">edit</a>

    {{ include('ligne_facture/_delete_form.html.twig') }}
{% endblock %}
