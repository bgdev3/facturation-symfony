{% extends 'base.html.twig' %}

{% block title %}LigneFacture{% endblock %}

{% block body %}
    <h1>LigneFacture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligneFacture.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ ligneFacture.designation }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ ligneFacture.quantite }}</td>
            </tr>
            <tr>
                <th>PrixUnitaireHT</th>
                <td>{{ ligneFacture.prixUnitaireHT }}</td>
            </tr>
            <tr>
                <th>TauxTVA</th>
                <td>{{ ligneFacture.tauxTVA }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ligneFacture.index') }}">back to list</a>

    <a href="{{ path('ligneFacture.edit', {'id': ligneFacture.id}) }}">edit</a>

    {{ include('ligne_facture/_delete_form.html.twig') }}
{% endblock %}
